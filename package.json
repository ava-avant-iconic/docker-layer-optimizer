{
  "name": "docker-layer-optimizer",
  "version": "1.0.0",
  "description": "CLI tool that analyzes Dockerfiles to identify inefficiencies in layer structure, caching, and image size",
  "main": "src/index.js",
  "bin": {
    "docker-layer-opt": "./bin/cli.js"
  },
  "scripts": {
    "test": "node --test",
    "lint": "eslint src/**/*.js",
    "format": "prettier --write src/**/*.js",
    "lint:fix": "eslint . --fix",
    "format:check": "prettier --check .",
    "audit": "npm audit"
  },
  "keywords": [
    "docker",
    "dockerfile",
    "optimization",
    "layers",
    "cache",
    "devops"
  ],
  "author": "AVA",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "chalk": "^5.3.0",
    "diff": "^5.2.0"
  },
  "devDependencies": {
    "eslint": "^9.0.0",
    "prettier": "^3.2.0"
  }
}